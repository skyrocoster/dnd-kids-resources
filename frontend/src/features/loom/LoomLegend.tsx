/** Legend section of the Weaver's panel, shown when nothing is selected.
 *  Renders keyed rows for: Woven update, Beacon (planned anchor), Reached anchor,
 *  Abandoned, "Now" (head/shuttle), "Next" (nearest future anchor).
 *  Each row is an icon/shape + label, never color alone.
 *  LU2 — Weaver's panel will implement the legend. */
export function LoomLegend() {
  return null
}
