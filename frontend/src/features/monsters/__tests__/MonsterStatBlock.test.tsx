import { describe, it } from 'vitest'

describe('MonsterStatBlock (X1)', () => {
  it.skip('renders all five regions in order', () => {
    // X1: full-featured monster shows Identity → Defenses → Abilities → Actions → Lore
  })

  it.skip('hides regions when their data is absent', () => {
    // X1: a bare beast hides Actions and Lore sections
  })

  it.skip('shows tapered teal rules between regions', () => {
    // X1: horizontal rules use teal gradient
  })
})
