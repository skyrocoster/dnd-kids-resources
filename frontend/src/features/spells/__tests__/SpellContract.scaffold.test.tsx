import { describe, it } from 'vitest'
import { targetSpell } from './spellFixtures'

describe.skip('spell target-contract consumers', () => {
  it('renders target browser metadata and casting-time alternatives', () => {
    void targetSpell
  })

  it('maps target editor form values to the create and update payloads', () => {
    void targetSpell
  })

  it('renders target player spell names and nested metadata', () => {
    void targetSpell
  })
})
