import { describe, it } from 'vitest'

describe('markerBadges', () => {
  it.skip('L1 — returns chips from passageStateChips in precedence order', () => {})
  it.skip('L1 — adds loot badge when loot is set', () => {})
  it.skip('L1 — adds trapDisarmed badge when disarmed', () => {})
  it.skip('L1 — returns empty array for unlocked with no loot', () => {})
})

describe('radialBadgeLayout', () => {
  it.skip('L1 — first badge at 12 o\'clock (-90°)', () => {})
  it.skip('L1 — even 180° spacing for 2 badges', () => {})
  it.skip('L1 — even 120° spacing for 3 badges', () => {})
  it.skip('L1 — even 90° spacing for 4 badges', () => {})
  it.skip('L1 — badges outside the marker radius', () => {})
})

describe('linearBadgeLayout', () => {
  it.skip('L3 — evenly spaced interior points along an N-facing segment', () => {})
  it.skip('L3 — evenly spaced along an E-facing segment', () => {})
  it.skip('L3 — normal offset pushes badges off the leaf', () => {})
})
