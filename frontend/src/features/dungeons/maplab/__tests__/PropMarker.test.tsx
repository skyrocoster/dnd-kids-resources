import { describe, it } from 'vitest'

describe('PropMarker', () => {
  it.skip('L1 — renders BadgeRing badges instead of corner-stacked badges', () => {})
  it.skip('L1 — renders correct count of radial badges', () => {})
  it.skip('L1 — no corner badge or loot badge when BadgeRing is active', () => {})
})
