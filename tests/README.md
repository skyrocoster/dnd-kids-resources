# Tests

- `e2e/` — Playwright browser tests (application, Storybook, accessibility)

Backend tests are colocated in [`backend/tests/`](../backend/tests/). The full
test process is documented in [`docs/FULL_TEST_PROCESS.md`](../docs/FULL_TEST_PROCESS.md).
